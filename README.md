# IZMAIL 50+ Railway runtime

Minimal public runtime shim used to start the private IZMAIL 50+ Bot application on Railway.

This repository contains no bot source code, Telegram token, database credentials, or production content. The application bundle and secrets are supplied only through protected Railway environment variables at runtime.
